spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nhk_test?serverTimezone=UTC
    username: root
    password: ${MYSQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
sentry:
  dsn: ${SENTRY_DSN}